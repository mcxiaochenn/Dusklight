---
/**
 * 主题切换组件
 * ─────────────────────────────────────────────────
 * 支持 light / dark / auto 三档切换。
 * 无闪烁主题恢复（内联脚本）。
 * View Transitions 兼容。
 */
---

<script is:inline>
  (function() {
    const theme = localStorage.getItem("theme") || "auto";
    const root = document.documentElement;
    root.classList.remove("light", "dark", "auto");
    root.classList.add(theme);
    if (theme === "auto") {
      const isDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
      root.classList.toggle("dark", isDark);
      root.style.colorScheme = isDark ? "dark" : "light";
    } else {
      root.style.colorScheme = theme;
    }
  })();
</script>

<button
  id="theme-toggle"
  type="button"
  aria-label="切换主题"
  class="theme-toggle"
  title="切换主题"
>
  <svg class="theme-toggle__icon theme-toggle__icon--sun" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
    <circle cx="12" cy="12" r="5"></circle>
    <line x1="12" y1="1" x2="12" y2="3"></line>
    <line x1="12" y1="21" x2="12" y2="23"></line>
    <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
    <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
    <line x1="1" y1="12" x2="3" y2="12"></line>
    <line x1="21" y1="12" x2="23" y2="12"></line>
    <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
    <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
  </svg>

  <svg class="theme-toggle__icon theme-toggle__icon--moon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
  </svg>

  <svg class="theme-toggle__icon theme-toggle__icon--auto" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
    <rect x="2" y="3" width="20" height="14" rx="2" ry="2"></rect>
    <line x1="8" y1="21" x2="16" y2="21"></line>
    <line x1="12" y1="17" x2="12" y2="21"></line>
  </svg>
</button>

<style>
  .theme-toggle {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: var(--radius-md);
    color: var(--foreground-secondary);
    transition:
      color var(--duration-fast) var(--ease-standard),
      background var(--duration-fast) var(--ease-standard);
  }

  .theme-toggle:hover {
    color: var(--foreground);
    background: var(--accent-subtle);
  }

  .theme-toggle__icon {
    position: absolute;
    transition:
      opacity var(--duration-fast) var(--ease-standard),
      transform var(--duration-fast) var(--ease-entrance);
  }

  .theme-toggle__icon--sun   { opacity: 1; transform: rotate(0); }
  .theme-toggle__icon--moon  { opacity: 0; transform: rotate(-90deg); }
  .theme-toggle__icon--auto  { opacity: 0; transform: rotate(90deg); }

  :root.dark .theme-toggle__icon--sun   { opacity: 0; transform: rotate(90deg); }
  :root.dark .theme-toggle__icon--moon  { opacity: 1; transform: rotate(0); }
  :root.dark .theme-toggle__icon--auto  { opacity: 0; transform: rotate(-90deg); }

  :root.auto .theme-toggle__icon--sun   { opacity: 0; transform: rotate(-90deg); }
  :root.auto .theme-toggle__icon--moon  { opacity: 0; transform: rotate(90deg); }
  :root.auto .theme-toggle__icon--auto  { opacity: 1; transform: rotate(0); }
</style>

<script>
  const ThemeManager = {
    getTheme(): string {
      if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
        return localStorage.getItem("theme")!;
      }
      return "auto";
    },

    getSystemTheme(): "light" | "dark" {
      return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
    },

    applyTheme(theme: string) {
      const root = document.documentElement;
      root.classList.remove("light", "dark", "auto");

      if (theme === "auto") {
        root.classList.add("auto");
        const systemTheme = this.getSystemTheme();
        root.classList.toggle("dark", systemTheme === "dark");
        root.style.colorScheme = systemTheme;
      } else {
        root.classList.add(theme);
        root.classList.toggle("dark", theme === "dark");
        root.style.colorScheme = theme;
      }

      localStorage.setItem("theme", theme);
    },

    init() {
      this.applyTheme(this.getTheme());
      window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change", () => {
        if (this.getTheme() === "auto") {
          this.applyTheme("auto");
        }
      });
    },

    toggleWithAnimation(originX: number, originY: number) {
      const current = this.getTheme();
      const next = current === "light" ? "dark" : current === "dark" ? "auto" : "light";

      // 计算从按钮位置到最远角落的距离
      const maxR = Math.hypot(
        Math.max(originX, window.innerWidth - originX),
        Math.max(originY, window.innerHeight - originY)
      );

      // 克隆 documentElement 作为遮罩层
      const overlay = document.documentElement.cloneNode(true) as HTMLElement;
      overlay.style.position = "fixed";
      overlay.style.inset = "0";
      overlay.style.zIndex = "9999";
      overlay.style.margin = "0";
      overlay.style.clipPath = `circle(0px at ${originX}px ${originY}px)`;
      overlay.style.transition = "clip-path 1s cubic-bezier(0.4, 0, 0.2, 1)";
      // 遮罩层应用新主题
      overlay.classList.remove("light", "dark", "auto");
      if (next === "auto") {
        overlay.classList.add("auto");
        const sys = this.getSystemTheme();
        overlay.classList.toggle("dark", sys === "dark");
      } else {
        overlay.classList.add(next);
        overlay.classList.toggle("dark", next === "dark");
      }
      document.body.appendChild(overlay);

      // 触发动画：圆形扩散
      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          overlay.style.clipPath = `circle(${maxR}px at ${originX}px ${originY}px)`;
        });
      });

      // 动画结束后切换真实主题并移除遮罩
      overlay.addEventListener("transitionend", () => {
        this.applyTheme(next);
        overlay.remove();
      }, { once: true });
    },

    toggle() {
      const current = this.getTheme();
      const next = current === "light" ? "dark" : current === "dark" ? "auto" : "light";
      this.applyTheme(next);
    },
  };

  ThemeManager.init();

  document.addEventListener("DOMContentLoaded", () => {
    const toggle = document.getElementById("theme-toggle");
    toggle?.addEventListener("click", (e) => {
      const rect = (e.currentTarget as HTMLElement).getBoundingClientRect();
      ThemeManager.toggleWithAnimation(rect.left + rect.width / 2, rect.top + rect.height / 2);
    });
  });

  document.addEventListener("astro:after-swap", () => {
    ThemeManager.applyTheme(ThemeManager.getTheme());
  });
</script>

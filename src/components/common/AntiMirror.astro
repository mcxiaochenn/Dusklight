---
/**
 * 防镜像站组件
 * ─────────────────────────────────────────────────
 * 源码始终可读。构建时 Vite 插件将 <!-- __ANTI_MIRROR__ -->
 * 替换为高度混淆的版本（随机变量名 + XOR 编码 + 死代码）。
 * 通过 siteConfig.antiMirror.enabled 控制开关。
 */
import { siteConfig } from "../../config/site";
const enabled = siteConfig.antiMirror.enabled;
---

<!-- __ANTI_MIRROR__ -->
